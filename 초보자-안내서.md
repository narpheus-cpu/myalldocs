# 서재 지도: 코딩을 몰라도 시작하는 안내서

명령어를 직접 입력할 필요가 없습니다. Windows에서 압축을 푼 뒤 **`시작하기.cmd`를 더블클릭**하세요. 도우미가 GitHub 로그인, 비밀값 등록, 프로젝트 업로드와 첫 인덱싱 실행을 대신합니다.

## 시작 전에 준비할 것 세 가지

1. Public으로 만든 GitHub 저장소 `narpheus-cpu/myalldocs`
2. Google AI Studio Free Tier Gemini API Key 한 개
3. Google Drive에서 책을 모아 둔 `[book]` 폴더

결제수단과 Cloud Billing은 필요하지 않습니다. 결제 연결을 요구하는 화면에서는 진행하지 마세요.

## 실제로 내가 할 일

1. `시작하기.cmd`를 더블클릭합니다.
2. 도우미가 Git 또는 GitHub 로그인 도구가 없다고 하면, 자동으로 열린 공식 페이지에서 설치합니다. 설치 뒤 `시작하기.cmd`를 다시 누릅니다.
3. GitHub 로그인 창에서 **Authorize**를 누릅니다.
4. 자동으로 열린 Google 화면에서 무료 프로젝트를 하나 고르고 **Google Drive API → 사용**을 누릅니다.
5. **서비스 계정 → 서비스 계정 만들기 → 키 → 새 키 만들기 → JSON**을 누릅니다. 역할 선택은 건너뛰어도 됩니다.
6. 도우미에서 방금 받은 JSON 파일을 고릅니다.
7. 도우미가 보여 준 `…gserviceaccount.com` 이메일을 복사하여 Drive의 `[book]` 폴더에 **뷰어**로 공유합니다.
8. Drive `[book]` 폴더 주소와 Gemini API Key를 도우미에 넣습니다.

여기까지 하면 도우미가 나머지를 처리하고 첫 작업을 시작합니다. 키와 서비스 계정 JSON은 프로젝트 파일에 복사되지 않고 GitHub의 암호화된 Secrets로 바로 전달됩니다.

## 작업 결과 확인

도우미가 마지막에 GitHub Actions 화면을 엽니다.

- 노란색 동그라미: 작업 중
- 초록색 체크: 완료
- 빨간색 엑스: 실패. 작업 이름을 눌러 표시된 한국어 오류를 확인

무료 Gemini 사용량이 다 차면 `PAUSED_RATE_LIMIT`으로 안전하게 멈춥니다. 다음 무료 한도 초기화 뒤 GitHub의 **Actions → Index books → Run workflow**를 누르면 저장 지점부터 이어집니다. 결제하거나 새 키를 만들 필요가 없습니다.

## 선택 기능: 사이트에서 Drive 폴더 고르기와 완료 이메일

처음부터 이 기능까지 설정할 필요는 없습니다. 기본 인덱싱이 한 번 성공한 뒤 [Apps Script 초보자 안내](apps-script/README.md)를 차례로 따라 하세요. 이 단계는 Google의 보안 정책 때문에 사용자가 직접 동의·배포 버튼을 눌러야 하며, 코드가 대신 누를 수 없습니다.

설정하지 않아도 GitHub Actions 화면에서 폴더 ID를 넣어 인덱싱하고 완성된 서재 사이트를 이용할 수 있습니다. 설정하면 사이트에서 폴더 선택 버튼을 사용할 수 있고, 선택 폴더의 모든 TXT/EPUB가 완료됐을 때만 `narepheus@gmail.com`으로 메일이 옵니다.

## 절대로 하지 말 것

- Cloud Billing 또는 Gemini Paid Tier 켜기
- Google Search Grounding이나 유료 검색 API 추가
- GitHub 저장소를 Private으로 바꾸거나 larger runner 선택
- API Key나 서비스 계정 JSON을 코드·README·게시글에 붙여넣기
- 무료 한도 초과를 해결하려고 키를 여러 개 돌려 쓰기

도우미는 위 조건을 위반하면 진행하지 않도록 만들어져 있습니다.
